{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Generate Graphs",
            "type": "shell",
            "command": "python3 generate_graph.py",
            "options": {
                "cwd": "${workspaceFolder}/"
            },
            "problemMatcher": []
        }
    ]
}